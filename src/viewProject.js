import { projects } from "./createNewProject";


function viewProject(currentProject) {
    console.log(currentProject);
    projects.forEach
}

function setCurrentProject (currentProject) {
    console.log(currentProject);
    projects.forEach
}

















export { setCurrentProject, viewProject }